# ==============================================================================
# src/core/config.py — ไฟล์ตั้งค่ากลางของแอปพลิเคชัน (Configuration)
# ==============================================================================
# ไฟล์นี้รวมค่าคงที่ (Constants) ที่ใช้ร่วมกันทั้งแอป เช่น:
#   - URL ของ API Server (Postman Mock Server)
#   - ชื่อแอปพลิเคชัน
#   - ชุดสีที่ใช้ในการออกแบบ UI (Color Palette)
# ข้อดี: เมื่อต้องการเปลี่ยนสี หรือเปลี่ยน API URL สามารถแก้ไขที่ไฟล์เดียว
#        โดยไม่ต้องไปแก้ทุกหน้าจอ
# ==============================================================================

# ──────────────────────────────────────────────
# URL ของ API Server — ใช้ Postman Mock Server เป็นตัวจำลอง API
# ทุก Service จะนำ URL นี้ไปต่อท้ายด้วย endpoint ต่างๆ เช่น /login, /student/home
# ──────────────────────────────────────────────
API_BASE_URL = "https://0e73cfd5-6b5f-4082-9c37-514cf7941cc1.mock.pstmn.io"

# ──────────────────────────────────────────────
# ชื่อแอปพลิเคชัน — แสดงบน Title Bar ของหน้าต่าง
# ──────────────────────────────────────────────
APP_TITLE = "Graduate Student Tracking System"

# ──────────────────────────────────────────────
# ชุดสีกลางของแอป (Color Palette)
# ใช้ Dictionary เก็บรหัสสีทั้งหมดไว้ที่เดียว เพื่อให้ทุกหน้าจอใช้สีเดียวกัน
# - primary        : สีหลักของแอป (สีชมพูแดง) ใช้กับปุ่ม, ไอคอน, หัวข้อ
# - background     : สีพื้นหลังหลักของหน้าจอ (ชมพูอ่อน)
# - form_background: สีพื้นหลังของหน้าฟอร์ม
# - white/black    : สีขาว/ดำ มาตรฐาน
# - text_dark      : สีข้อความเข้ม สำหรับเนื้อหาหลัก
# - text_muted     : สีข้อความจาง สำหรับ label หรือข้อมูลรอง
# - border/border_light : สีขอบ Container
# - divider        : สีเส้นแบ่ง
# - card_shadow    : สีเงาของการ์ด (มีความโปร่งใส)
# - icon_bg        : สีพื้นหลังไอคอน
# - grey_bg        : สีเทา สำหรับกล่องพื้นหลัง
# - gradient_start/end : สีเริ่มต้น-สิ้นสุดของไล่สี (Gradient) ในส่วน Header
# ──────────────────────────────────────────────
APP_COLORS = {
    "primary": "#EF3961",
    "background": "#FFF6FE",
    "form_background": "#FFF5F7",
    "white": "#FFFFFF",
    "black": "#000000",
    "text_dark": "#333333",
    "text_muted": "#888888",
    "border": "#E5E5E5",
    "border_light": "#F0F0F0",
    "divider": "#EEEEEE",
    "card_shadow": "#08000000",
    "icon_bg": "#F5F5F5",
    "grey_bg": "#E0E0E0",
    "gradient_start": "#FF9EBB",
    "gradient_end": "#FFB6C8",
}
